{
  "buildCommand": "cd neon-guard-ui-main && npm install && npm run build",
  "outputDirectory": "neon-guard-ui-main/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
